apiVersion: v1
kind: Service
metadata:
  labels:
    app: hawtio
  name: hawtio-online-gateway
  namespace: hawtio-dev
spec:
  ports:
  - name: hawtio-online-gateway
    port: 3000
    protocol: TCP
    targetPort: express
  selector:
    app: hawtio
    deployment: hawtio-online
  type: NodePort
